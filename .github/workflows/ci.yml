name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  ci:
    name: Lint, security, test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7.0.0

      - uses: actions/setup-python@v6.3.0
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -e ".[dev,memory,adapters]" black bandit hypothesis

      - name: black
        run: black --check .

      - name: bandit
        run: bandit -r fieldworks/ -ll

      - name: pytest
        run: pytest
